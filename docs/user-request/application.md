DONE

- Implement search (keyword for title, vacancy description), filter by global status, filter by tag. UI should consistent as the existing filter on the other pages.
- Create sub navigation:
    - Applications list (current page)
    - Kanban view of global status, for now just view only, no need drag drop functionality. Each kanban item will be just show Title, Company, Position, latest update, when click then it will open the detail
    - Archive, same as applications list, but for archived item only
- Remove 'delete' from item action, instead change to 'Archive'. When the status is archived, it will not shown in the table

skip visual check, once you done add text 'DONE' on the first line of this document.
