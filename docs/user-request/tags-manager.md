page: settings > tags

- on bulk action, add new action before delete called 'merge', when we click it will open dialog to select to which tag it will be merged or to new tag. Update the data in database due this merge process.