# Progress

Running index of every initiative tracked in `docs/plans/`. Update this as
phases complete during `/do` execution — don't let it drift from reality.

| # | Initiative | Spec | Plan | Status | Notes |
|---|---|---|---|---|---|
| 01 | App taxonomy | [specs/01-app-taxonomy.md](specs/01-app-taxonomy.md) | — | Settled | Sections, Inventory pages, Inventory→CV→snapshot model, selection rules. 4 weaknesses deferred |
| 02 | Inventory data model | [specs/02-inventory-data-model.md](specs/02-inventory-data-model.md) · [diagram](specs/02-inventory-schema-diagram.md) | — | Draft — audited | **5** tables (4 until 11 added `tags`), unified item model. `item_kind` now 17 values — Basics became six pools, `profiles` shrank to an account anchor, `social_profiles` folded in. Covers all 6 scenarios. Needs sign-off on unified vs normalized |
| 03 | App shell + route scaffold | [specs/01-app-taxonomy.md](specs/01-app-taxonomy.md) | — | Done — placeholders | Vite + Tailwind v4 + shadcn (base-nova). react-router, sidebar shell, all 20 routes. Every page is a placeholder — no data, no forms. Nav manifest at `src/lib/navigation.ts` drives sidebar, breadcrumb, headers, Inventory index. Closes taxonomy weakness #3 (Inventory index page) |
| 04 | Templates gallery | [specs/01](specs/01-app-taxonomy.md) | — | Superseded by 08 | New top-level **Templates** section at `/templates`. Started as 4 A4 layout-skeleton mockups; replaced by real renders in 08 |
| 05 | Basics page on mock data | [specs/01](specs/01-app-taxonomy.md) · [specs/02](specs/02-inventory-data-model.md) | — | Done | First page wired to `src/mocks/`. Line-variant tabs, one table per pool, columns per pool via `PoolTable`. Read-only — no editing, add buttons inert. Pattern to follow for the other 12 pool pages |
| 06 | `note` + uniform timestamps | [specs/02](specs/02-inventory-data-model.md) · [diagram](specs/02-inventory-schema-diagram.md) | — | Done — spec + mocks | `note text` on `inventory_items` and `inventory_lines` (private, stripped on export). `created_at`/`updated_at` added to `inventory_lines` and `item_skills` so all four tables match; `touch_updated_at` trigger extended. Mock types/flatten carry `note`; mocks still have no timestamp fields |
| 07 | All Inventory pool pages on mock data | [specs/01](specs/01-app-taxonomy.md) · [specs/02](specs/02-inventory-data-model.md) | — | Done | 11 remaining pools wired to `src/mocks/`. Each page is 4 lines over `PoolPage`; columns per kind in `pool-columns.tsx`, shared builders in `columns.tsx`. Search + checkbox selection + bulk bar + row actions come from `PoolPanel`/`PoolTable`. Read-only; every action inert. Import/Export still a placeholder |
| 08 | Soft delete | [specs/02](specs/02-inventory-data-model.md) · [specs/01](specs/01-app-taxonomy.md) | — | Spec only | `deleted_at` on `inventory_items` and `inventory_lines`; `item_skills` deliberately excluded. `live_items`/`live_lines` `security_invoker` views, no cascading trigger, partial indexes, 30-day purge. Not implemented — mocks and UI unchanged. Open: where Trash lives (spec 01 Q3), retention window |
| 09 | CV selection + real templates | [specs/03-cv-selection.md](specs/03-cv-selection.md) | [plans/03](plans/03-cv-selection-and-templates.md) | Done — mocks | New spec 03 (`cvs`/`cv_sections`/`cv_items`/`cv_lines`). Two mock CVs prove the premise — same Inventory, different bullets and section order. `buildResumeDocument()` is the swap-to-Supabase seam. 4 real templates + A4 print CSS. `/cvs` lists CVs, `/cvs/:id/print` previews one. No CV builder yet — selections are authored in mocks |
| 10 | Template is preview-only | [specs/03-cv-selection.md](specs/03-cv-selection.md) | — | Superseded by 16 | **Reverses 09's template binding.** `cvs.template_id` dropped — a CV is content, layout is a preview choice. Template picker on the CV preview, CV picker on the Templates gallery; neither persists. The binding that matters ("which layout did Globex receive?") moves to Applications, unspecced. Row 16 reopens this: a persisted (Persona, Template) pairing is now allowed to exist as a saved CV, alongside the still-free ad hoc preview |
| 11 | Tag registry + Settings tags UI | [specs/02](specs/02-inventory-data-model.md) · [diagram](specs/02-inventory-schema-diagram.md) | — | Done — spec + mocks | **Promotes tags to a real `tags` table** — the 5th table. Arrays on `inventory_items`/`inventory_lines` stay the query path but may only hold registered names; no FK is possible on an array element, so an `assert_tags_registered()` trigger enforces it. Names are `^[a-z0-9]+$` (`open-source` → `opensource`, `civic-tech` → `civictech` in mocks). Settings is now tabbed: **Tags** (add / rename / delete / bulk delete, usage counts) + General placeholder. `src/mocks/tags.ts` is the registry; `SearchInput` extracted from `PoolPanel`. Every Inventory pool also gains a **tag filter** next to search — shadcn `Combobox` chips, prefix-matched suggestions capped at 5, AND semantics; suggestions are drawn from the rows still matching, so no choice is a dead end. Both filters persist per pool via `useSessionState` (sessionStorage). Not verified in a browser |
| 12 | Basics add/edit forms | [specs/02](specs/02-inventory-data-model.md) | [plans/04](plans/04-inventory-basics-forms.md) | Done — verified in browser | Real add/edit for the Basics pool's six kinds, replacing the disabled Add/Edit/Delete controls on `PoolPanel`/`PoolTable`. Adds `createItem`/`updateItem`/`deleteItem` mock mutators, plus two reusable components used by every future pool form: `TagInput` (registry-only picker) and `NoteInput`. Uses shadcn Input Group; no form library added, follows the existing `Field`+`useState` pattern. `PoolPanel`/`PoolTable` take optional `formKind`/`onDataChanged`/`onEditRow`/`onRequestDelete` props — only Basics passes them, every other pool (via `PoolPage`) keeps Add/Edit/Delete disabled exactly as before. Verified add/edit/delete round-trip live in the browser on `/inventory/basics`; confirmed `/inventory/work` unaffected. Basics only — the other 12 pools (which have nested `inventory_lines`) are a follow-up once this is validated |
| 13 | Supabase integration | [specs/04](specs/04-supabase-integration.md) · [02](specs/02-inventory-data-model.md) · [03](specs/03-cv-selection.md) | [plans/05](plans/05-supabase-integration.md) | Phases 1–6 done except CVs; Phase 7 blocked on that | Real project "Tailored CV" (`artmbeeadepxggbvfkdr`). Schema = spec 02 + 03 **minus soft delete**, 9 tables, RLS on all, zero advisor findings. **Found and fixed two real bugs while wiring this up**: (1) spec 02's `assert_tags_registered()` COALESCE branch referencing `new.item_id` fails on `inventory_items` rows ("record new has no field item_id") — PL/pgSQL binds every RECORD field reference in an embedded query eagerly, before that query's own short-circuit runs — fixed with `tg_table_name` branching (migration `20260805194200`), spec 02's SQL block is now stale and needs the same fix reflected there; (2) the login page never redirected after a successful sign-in — `RequireAuth` only guards the *other* routes, nothing redirected away from `/login` itself once a session existed, fixed in `src/pages/login.tsx`. Single-account email/password auth wired and verified live (login → dashboard → sign out). Data-access layer (`src/lib/inventory.ts`/`tags.ts`/`cv.ts`/`inventory-store.tsx`) ports the mock selectors verbatim onto a fetch-once-per-session store, staying synchronous since `columns.tsx` calls them inside table cells. Seed script (`scripts/seed.ts`, signs in as the real account instead of using a service-role key — none is exposed via MCP) has run once: 68 items / 165 lines / 45 skill links / 47 tags / 2 CVs, exact match. **Cut over and verified live in browser**: shared pool infra (`PoolPanel`/`PoolTable`/`ItemDetailDialog`/`columns.tsx`), Basics (read + write, favourite-toggle round-trip confirmed against the live DB), Tags registry (Settings), and all 13 Inventory pool list pages. **Deliberately deferred**: the CVs cutover (`cvs.tsx`, `cv-print.tsx`, `templates.tsx`, `add-to-cv-dialog.tsx`, `ResumeDocument`/`ResumeSection`/`ResumeEntry` types, plus the `@/mocks/cv` imports still left in `item-detail-dialog.tsx`/`columns.tsx`) — those files overlap with the separate, concurrent CV template JSON format work (row 14); revisit once that lands. `src/mocks/` (non-cv parts) is now unused by the app outside `scripts/seed.ts` |
| 15 | Add/edit/delete for remaining pools | [specs/02](specs/02-inventory-data-model.md) | [plans/07](plans/07-inventory-pool-forms.md) | Done — verified in browser | Follow-up to row 12's Basics forms. Covers Education, Skills, Languages, Interests, Projects, Publications, Awards, Certificates, References, and Volunteer (10 kinds) — `work` explicitly excluded, deferred as its own follow-up. One shared `ItemDialog` (renamed/generalized from `BasicsItemDialog`) serves every kind, including a new `PartialDatePicker` (shadcn `Calendar`+`Popover`+`ToggleGroup`, since dates are partial-ISO — year, year-month, or full day — not plain calendar dates). `src/lib/inventory.ts` gained `startDate`/`endDate`/`yearsExperience` on `createItem`/`updateItem` (previously unwritten), plus `createLine`/`updateLine`/`deleteLine`/`replaceItemSkills` and two new reusable components (`SkillLinkInput`, `LineListEditor`). Also fixed a pre-existing gap where `ItemDetailDialog`'s own Delete menu item was hard-disabled and never wired, and a data-loss bug caught live in browser verification: a kind's field config omitting a column (e.g. Reference's `subtitle`/"Role", which real seed data populates) defaulted it to `null` in every save instead of leaving it untouched — fixed by defaulting unconfigured fields to `undefined` so `updateItem`'s merge-patch skips them. Also fixed `npm run typecheck` itself, which silently checked nothing all session (`tsc --noEmit` resolved to the root solution `tsconfig.json`, `files: []`) — now `tsc -b`, matching what `build` already ran. Verified live: Award (flat, single-date) and Volunteer (date range + two line kinds + skill link) full add/edit/delete round-trips; all 10 kinds' Add dialogs spot-checked for correct field sets; `work` confirmed still fully disabled |
| 14 | CV template JSON format v2 + real PDF | [specs/07](specs/07-cv-template-pdf-format.md) (supersedes [05](specs/05-cv-template-format.md)) · [03](specs/03-cv-selection.md) | [plans/10](plans/10-cv-template-pdf-format.md) (supersedes [plans/06](plans/06-cv-template-format.md), never executed) | Done | Shipped the `json-ui`-shaped format: named `styles` registry with `extends`, `blocks` registry addressed via `$data`/`$prop`/`$item`/`$index` sigils, point units, `page: PageConfig` inside `TemplateDefinition`. New shared `src/lib/cv-template-core.ts` (framework-free `resolveValue`/`resolveStyleObject`) backs both a rewritten DOM preview backend (`template-node-renderer.tsx`) and a new real `@react-pdf/renderer` PDF backend (`template-pdf-renderer.tsx`, dependency `^4.5.1`). `cv-print.tsx` replaced `window.print()` with a `<PDFViewer>` + `<PDFDownloadLink>` real download page — verified in-browser producing a genuine multi-page, selectable-text PDF. Classic is the only ported template (Sidebar/Compact/Academic remain unbuilt, out of scope). Settings plumbing (`TemplateSettings`) present but unwired, per plan. Two real bugs found and fixed during live verification: (1) the 6 structural node schemas needed `.strict()` — without it, zod's `z.union` silently matched every node shape against the all-optional `ElementNode` schema first, stripping `if`/`repeat`/`block`/`join`/`pageNumber` keys down to `{}`; (2) `resolveValue` didn't handle bare `$item`/`$data`/`$prop` sigils (no dot-path), needed for `repeat.as` over plain arrays like `contactParts`. `npm run typecheck && npm run lint && npm run build` all clean. |
| 16 | Persona / CV split | [specs/06-persona-cv-split.md](specs/06-persona-cv-split.md) · [03](specs/03-cv-selection.md) | [plans/08](plans/08-persona-cv-split.md) | Mocks + nav + pages done; DB migration + seed re-run still pending | **Nav/IA landed differently than plan 08 drafted, per live direction during `/do`:** Personas promoted to its own top-level sidebar item (not nested under CVs) — `Dashboard, Personas, CV, Profile Inventory, Applications`. "Profile" section relabeled "Profile Inventory" (title only, code/routes still say `/inventory`, drift deliberately untouched). Templates demoted from a standalone top-level section into a tab on the CV page (`CV List` / `Templates`, `src/pages/cv.tsx`, mirrors `settings.tsx`'s Tabs pattern) — `sections.templates` removed from `navigation.ts` entirely. Settings removed from the sidebar (footer) since it's already one click away via the account popup (`user-menu.tsx`); route/page untouched. **Mock/data layer**: `src/mocks/cv.ts` → `src/mocks/persona.ts` (11 exports renamed: `allCvs`→`allPersonas`, `findCv`→`findPersona`, `cvsUsingItem`→`personasUsingItem`, `cvUsageCount`→`personaUsageCount`, `ResumeDocument.cvId/cvName`→`personaId/personaName`, etc.), `mocks/data/cvs.ts`→`mocks/data/personas.ts`, `mocks/types.ts`'s `DbCv*`/`SourceCv*` → `DbPersona*`/`SourcePersona*`. New `src/mocks/cv.ts` + `src/mocks/data/cvs.ts` for the actual new entity — a saved (Persona, Template) pairing (`personaId`, `templateId`, `name`, `note`), plus a `resolveCv()` helper composing persona content + template lookup for the print page. Dead `src/lib/cv.ts` deleted (zero imports, already stale). Dependent files fixed: `item-detail-dialog.tsx`/`columns.tsx` ("In CVs"→"In Personas"), `add-to-cv-dialog.tsx`→`add-to-persona-dialog.tsx`, 5 files under `components/cv/` (import path only). **Persona detail page** added as a follow-up in the same session: `src/pages/persona-detail.tsx` at `/personas/:id`, read-only (name/headline/summary/contact, each section's entries with line groups and skills via `buildResumeDocument`, plus a reverse-lookup "Used in CVs" list) — no template/layout involved, consistent with Persona being content-only. `PersonasPage`'s name cell and `item-detail-dialog.tsx`'s usage-list link both now point at this per-persona route instead of the flat `/personas` list. **Not done this session** (explicitly deferred, matches plan 08 phases 1 and 5): the DB migration renaming `cvs`/`cv_sections`/`cv_items`/`cv_lines`→`persona_*` and creating the new `cvs` table live in Supabase; `database.types.ts` regeneration; re-running `scripts/seed.ts` against the new schema. `scripts/seed.ts` was still touched — fixed to compile against the renamed mock exports while continuing to write into the *current, unmigrated* `cvs`/`cv_sections`/`cv_items`/`cv_lines` tables, since those still hold the old content-selection shape live. `npm run typecheck`, `lint`, and `build` all pass. **Not verified in browser** — user opted to check manually |
| 17 | Persona/CV Supabase cutover + inline picker | [specs/06](specs/06-persona-cv-split.md) · [03](specs/03-cv-selection.md) | [plans/09](plans/09-persona-cv-cutover.md) | Done — verified live | **Finishes what row 16 deferred, plus a new create/edit feature.** All 8 phases executed and verified against the real Supabase project (`artmbeeadepxggbvfkdr`), not mocks. **DB**: migration applied live — renamed `cvs`/`cv_sections`/`cv_items`/`cv_lines`→`personas`/`persona_sections`/`persona_items`/`persona_lines` (data-preserving rename, all rows survived), added `favorite`/`tags` columns to `personas`, created the new `cvs` table (saved Persona+Template pairing), backfilled the 2 pre-existing personas' `cvs` rows via a name-keyed `insert...select` (no hardcoded UUIDs). `database.types.ts` regenerated. **Data layer**: new `src/lib/persona-store.tsx` (`PersonaStoreProvider`/`usePersonaStore()`, mirrors `inventory-store.tsx`, one combined store for personas+sections+items+lines+cvs), `src/lib/persona.ts` (selectors ported from the old mock, plus real mutators — `createPersona`, `togglePersonaFavorite`, and `setPersonaSectionItems`, the picker's core "confirm" mutator: full-replaces one section's item selection, cascades removed items' lines automatically via FK, defaults newly-added items to all their lines), `src/lib/cv.ts` (`allCvs`/`findCv`/`resolveCv`, selectors only — no CV creation this pass). `src/mocks/persona.ts` and `src/mocks/cv.ts` deleted (confirmed fully dead); `mocks/data/personas.ts`/`mocks/data/cvs.ts`/the `Source*` types stay for `scripts/seed.ts`. **Picker infra**: `POOL_COLUMNS` widened from `Partial` to total (Basics' 6 kinds moved in from `basics.tsx`), new `usePoolData(kind)` hook shared by `PoolPage` and the picker, `PoolColumn.cell` gained a 4th `persona: PersonaStore` param (`PoolTable` now calls both stores' hooks), `PoolPanel` gained `mode="pick"`/`selectionMode`/controlled `selected` (every existing call site untouched, defaults preserved), new `PoolPickerDialog` (`sm:max-w-6xl max-h-[90vh]`, new size precedent — nothing in the app exceeded `sm:max-w-3xl` before). **Pages**: `personas.tsx`'s "New Persona" now opens a minimal name-entry dialog (`new-persona-dialog.tsx`) and navigates to the new persona; `persona-detail.tsx` fully reshaped — Hero split into 3 cards (Name/Headline/Summary, matching Contact/Location/Social), all 17 cards (6 Basics + 11 pools) always render now (populated or empty-state with a "+"), each wired to the shared `PoolPickerDialog`. `cv-list-panel.tsx`/`templates-panel.tsx`/`cv-print.tsx`/`add-to-persona-dialog.tsx`/5 `components/cv/*.tsx` files mechanically swapped from mocks to the new lib. **Verified live in browser + via `execute_sql`**: full create→pick→confirm→reopen(pre-checked)→swap-selection round trip on a real test persona (cleaned up after), cascade-delete of removed items' lines confirmed with zero orphans, pick-one Basics kind (Name) confirmed replace-not-add both in UI and data, `/inventory/work`+`/inventory/basics` regression-checked after the shared-infra changes, `/cvs` list + Templates tab + both seeded CVs' print pages all render against live data, "Used in Personas" reverse-lookup confirmed in the Inventory item detail dialog. Zero console errors throughout. `npm run typecheck`/`lint`/`build` all clean at every phase gate. **Deferred, per locked-in scope**: per-bullet/line curation in the picker (whole-entry only), Persona-level Edit(rename/note)/Duplicate/Delete (stay disabled placeholders), DB-level enforcement of pick-one Basics kinds (app-layer only, mutator throws), `cvs.template_id` real FK (no `cv_templates` table yet) |
| 18 | Persona print settings (Visibility/Data/Style/Page) | [specs/08-persona-print-settings.md](specs/08-persona-print-settings.md) · [06](specs/06-persona-cv-split.md) | — | Done — typecheck/lint clean, not browser-verified | **No plan doc** — built directly against live turn-by-turn direction on the `native-print` branch, spec written after the fact (see spec 08 for full rationale). Replaces the CV detail page's empty sidebar slot with `components/cv/persona-field-tree.tsx`, a 4-tab settings panel. **DB**: two new `jsonb` columns, both default `'{}'` — `personas.field_visibility` (per-kind `hidden`/`fields[]`/`items{id:hidden}`) and `cvs.template_settings` (typed by the pre-existing-but-unwired `TemplateSettings` — `styles`/`page`/`nodes`; `nodes` stays unwired). **Visibility tab**: kind/field structural hide (`FIELD_REGISTRY`, restricted to fields the Classic template actually renders) plus Section reorder (`persona_sections.position`, full-list upsert per move so every kind gets an explicit row). **Data tab**: non-destructive per-entry hide (`field_visibility.items`, deliberately not `persona_items` deselection — that cascades and loses bullet curation) and per-bullet curation reusing `persona_lines` membership directly (already non-destructive, no second flag needed). **Style tab**: per-CV overrides on the current Template's named styles, manifest-driven off new `lib/style-property-schema.ts`, explicitly modeled on `json-ui`'s `StyleEditor.jsx`/`propertySchema.json` (`~/Desktop/json-ui`) adapted for point-based (not px/em/%) values; grouped property rows with per-property reset-to-template. **Page tab**: same approach over the fixed field set in new `lib/page-property-schema.ts` (paper size, orientation, margin, header/footer space, base typography) — `header`/`footer` excluded (structured `TemplateNode` content, no scalar editor). Both overrides shallow-merge onto the template's own values at render time (`cv-template-core.ts`'s `resolveStyleObject`; a new merge in `TemplateNodeRenderer` for `page`) — template code itself never mutated. Fixed two previously-dead fields while wiring this up so their new toggles actually do something: `PageConfig.orientation` (defined, never read — now swaps page width/height for landscape) and `ResumeEntry.url` (defined, never rendered by Classic — added a conditional line to `entryBlock`). Also: the CV detail page now force-collapses the app's global sidebar to icon-only for its own duration (`useSidebar()`, restored on unmount), and cleaned up leftover dead imports/`any` typing in `cv-print.tsx` from the branch's earlier (pre-session) switch off `@react-pdf/renderer`'s `<PDFViewer>` to native `window.print()` via `react-to-print`. `npm run typecheck`/`lint` clean throughout; **not verified in a browser** — user opted to check manually rather than via Chrome DevTools this session. **Deferred**: `TemplateSettings.nodes` (per-node hidden override) stays unwired; `template-pdf-renderer.tsx` (the `@react-pdf/renderer` backend) doesn't read `templateSettings` — only the DOM/native-print path does |
| 19 | Batch 1 — skill categories + template interpolation/merge | [specs/07](specs/07-cv-template-pdf-format.md) | — | Done — verified live | **Driven by `docs/user-request.md`'s batch backlog, not a spec-first flow** — user explicitly asked to execute in batch order and align docs afterward; plan captured in-session (`EnterPlanMode`) rather than a `docs/plans/` artifact. Three independent pieces. **(1) Skill categories**: new `skill_categories` table (`id`/`user_id`/`name`/`position`, unique per user) + nullable `inventory_items.category_id` FK (`on delete set null`, checked to skill-kind rows only) — migration `20260810080000_add_skill_categories.sql`, applied live, types regenerated. `src/lib/skill-categories.ts` mirrors `src/lib/tags.ts`'s CRUD shape but simpler (FK rename is one `UPDATE`, no cascade rewrite). Settings gained a third tab, **Skill Categories** (`skill-categories-panel.tsx` + `skill-category-dialogs.tsx`, cloned from the Tags panel/dialogs, no bulk actions — single FK, no batch-tag workflow to mirror). `item-dialog.tsx`'s skill form gained a Category `Select` (store-backed, "No category" sentinel maps to `null`). **(2) String-literal interpolation**: `cv-template-core.ts`'s `resolveValue` now has two modes — whole-string sigil (unchanged: returns the raw resolved type, needed by `repeat.for`/`if`/etc.) vs. embedded sigils in a literal string (new: each `$data.x`/`$prop.x`/`$item.x`/`$index` token is resolved and `String()`-coerced in place, e.g. `"Hello, $data.name"` → `"Hello, Ada"`). Shared by both backends' `text` resolution — no renderer changes needed. **(3) Merge mode for `repeat`**: new optional `repeat.merge: {text, separator?, end?}` on the schema (`.strict()`-safe addition); when set, `template-node-renderer.tsx`'s `renderRepeat` renders one joined string (e.g. `"React, Vue, Svelte."`) instead of one block instance per item. **`template-pdf-renderer.tsx` (the `@react-pdf/renderer` backend) is confirmed dead code** (zero imports — `cv-print.tsx` uses `react-to-print` over the DOM renderer per the `native-print` branch) and was left untouched; merge is DOM-backend-only. Per the request, no existing template's JSON was changed to *use* merge — that's explicitly deferred ("this setting will be implemented on the next batch"). Instead, a new demo-only template (`cv-template-defs/batch1-demo.ts`, id `batch1-demo`, registered in `cv-templates.ts`) exercises both features live: an interpolated intro line and a merged Skills section — a live test surface, not a real layout choice. **Sample data added at the user's invitation** ("add data and template if you want so I can also test it, we are not live yet"): 4 real skill categories (Languages/Frameworks & Libraries/Databases & APIs/DevOps & Tools) created and assigned across all 20 existing skill rows via direct SQL, plus a "Batch 1 demo test" CV left in the CV list against the `batch1-demo` template. **Docs updated**: spec 07's "Scope and placeholders" section and `RepeatNode` type now describe both new capabilities. `npm run typecheck`/`lint` clean (only 2 pre-existing, unrelated `TS2769` errors in `template-node-renderer.tsx` remain, same ones noted in row 14). **Verified live in browser**: Settings → Skill Categories tab (add/rename/delete/usage-count round-trip against real Supabase, `on delete set null` confirmed clearing the FK), skill edit dialog's Category select (persisted and reopened correctly), and the `batch1-demo` template's print preview (interpolated text and merged Skills paragraph both rendered correctly against real persona data, zero console errors) |
| 20 | Batch 3 — persona settings on CV, self-contained templates, schema labels, per-instance Block tab | [specs/07](specs/07-cv-template-pdf-format.md) · [08](specs/08-persona-print-settings.md) | — | Done — verified live | Continues the `docs/user-request.md` batch backlog (row 19 was Batch 1; Batch 2 doesn't exist in the backlog — user confirmed "batch 2" meant "the next one," i.e. Batch 3). Plan captured in-session (`EnterPlanMode`), not a `docs/plans/` artifact, per the same user direction as row 19. Four linked pieces. **(1) Field visibility moved off `personas` onto `cvs`**: migration `20260810090000_move_field_visibility_to_cvs.sql` — added `cvs.persona_settings jsonb`, backfilled each CV from its Persona's old `field_visibility` (`jsonb_build_object('fieldVisibility', ...)`), dropped `personas.field_visibility`. New `mocks/types.ts` types `FieldVisibility`/`CvPersonaSettings` (moved from `persona-store.tsx` to the base types layer to avoid an import cycle — `cv.ts`/`persona-store.tsx` both need it and both import `DbCv` from `mocks/types.ts`). `buildResumeDocument` (`persona.ts`) gained an optional 4th `fieldVisibility` param instead of reading `found.fieldVisibility`; only `cv.ts`'s `resolveCv` passes one (`cv.personaSettings.fieldVisibility`) — the 3 no-CV-context callers (Templates gallery preview, Persona's own read-only page, quick-create preview) now correctly render the full, untailored Persona. `setKindHidden`/`setFieldHidden`/`setItemHidden` moved from `persona.ts` (personaId-keyed, wrote `personas`) to `cv.ts` (cvId-keyed, write `cvs.persona_settings`) — `persona-field-tree.tsx`'s `VisibilityTab`/`DataTab`/`KindRow`/`ItemRow` switched from a `personaId` prop to a `cv: DbCv` prop accordingly. Section order (`persona_sections.position`) and per-bullet curation (`persona_lines`) deliberately **not** moved — neither is part of `field_visibility`'s jsonb, and the request only named field visibility. **Verified live**: toggled Summary hidden on one CV, confirmed a second CV built from the *same* Persona still shows it — the actual bug this fixes. **(2) `cv-template-blocks.ts` deleted**: its 7 shared blocks inlined as local consts into `classic.ts`, `two-column.ts`, and `batch1-demo.ts` each (near-duplicates by design, not re-shared) — every template def is now a fully self-contained file. **(3) `stylesSchema`/`blocksSchema`**: new optional `Record<string, {title, description?}>` maps on `TemplateDefinition` (+ matching `.strict()`-safe Zod), authored in full for `classic.ts`/`two-column.ts` (every style/block key) and lightly for `batch1-demo.ts`. Fixes the Style tab's label bug (`persona-field-tree.tsx:687` was using raw keys like `headerName` as dropdown labels) — now `stylesSchema[name]?.title ?? humanize(name)` (`humanize` exported from `style-property-schema.ts`), plus the style's `description` shown as helper text. **(4) New "Block" tab** (5th tab, initially "Block Settings" — shortened to "Block" per live feedback): overrides one specific **node instance** in the template tree (confirmed with the user via `AskUserQuestion` — per-instance, not per-block-name, since two different `repeat`s using the same block should be independently overridable). Extended the pre-existing-but-unwired `TemplateSettings.nodes` from `{hidden?}` to `{hidden?, styles?, text?}` — `text` runs through the same interpolation-aware `resolveValue` any node's `text` does, so an override can be a sigil to rebind the node to different data, not just literal replacement. New `src/lib/cv-template-core.ts`'s `collectBlockNodeIds(definition)` walks `root` + every reachable `blocks` entry, returning every `{id, blockName, hasText}` (dedup'd by id) — for a `block`/`repeat` node's own id, `hasText` follows the *instantiated block's root node*, since `template-node-renderer.tsx`'s `renderBlockInstance` patches that root (not the styleless wrapper) on override. `classic.ts`/`two-column.ts`/`batch1-demo.ts` each mark a small, deliberate id set: `bulletMarker`, `keywordSeparator`, `contactSeparatorText`, `sectionHeadingInstance`, `entryBlockRepeatTight`/`Spaced` (two-column also: `mainSectionsRepeat`/`secondarySectionsRepeat`) — addressing one node in the tree (e.g. Skills' entry container independently of Work's), not one rendered row out of the N a single repeat draws. New `cv.ts` mutators `setCvNodeOverride`/`resetCvNodeOverride`, same shallow-merge-onto-`cvs.template_settings` pattern as the Style/Page tab mutators. **Verified live**: overrode the `batch1-demo` CV's contact separator from "|" to "-", confirmed it rendered immediately and a different CV on the same template was unaffected; reset afterward. **Docs**: spec 07 (`TemplateDefinition`'s `blocks`/`stylesSchema`/`blocksSchema` fields, the extended `TemplateSettings.nodes` shape, `cv-template-blocks.ts`'s removal, confirmed-dead-code note on the PDF backend) and spec 08 (data model section rewritten for `cvs.persona_settings`, UI table gained the Block tab row, mutator list updated) both updated in place rather than forked. `npm run typecheck`/`lint` clean throughout (same 2 pre-existing unrelated `TS2769` errors as rows 14/19, at shifted line numbers). Zero console errors across all browser verification. |
| 21 | Batch 4 — Classic template compactness + ATS friendliness | [specs/07](specs/07-cv-template-pdf-format.md) | — | Done — verified live | Continues the `docs/user-request.md` batch backlog, Classic template only. Plan captured via `EnterPlanMode` (not a `docs/plans/` artifact), same convention as rows 19–20. Also fixed the pre-existing `npm run typecheck` failure flagged as row 20's next step (`template-node-renderer.tsx`'s two `createElement(tag, ...)` calls: `tag` is `BoxTag \| undefined`, defaulted to `"div"` at the call site — `tsc -b` now clean). **Data layer** (`src/lib/persona.ts`, additive — `two-column.ts`/`batch1-demo.ts`/`persona-detail.tsx` untouched and unaffected): `ResumeEntry` gained `bulletItems: string[]` (every `lineGroups` group flattened into one list, computed in `toEntry`). New `ResumeSkillGroup` type + `ResumeSection.skillGroups?` (skill sections only) — new `buildSkillGroups` helper groups a skill section's already-visibility-filtered items by `inventory_items.category_id`, ordered by `skill_categories.position` (uncategorized skills collected last under `"Other"`); wired into `buildResumeDocument`'s `sections` map. `buildResumeDocument`/`resolveCv` (`cv.ts`) parameter types widened from `InventoryData` to `InventoryStore` (`skillCategories` lives only on the latter; every real call site already passes the full store, so this was a pure type-signature fix, no behavior change). **Template layer** (`classic.ts` only): (1) header contact line — `contactPart` repeat switched from a `<ul>`/`<li>`-per-part-plus-separator-glyph to `repeat.merge` (`" \| "` separator), rendering one plain-text `<p>` instead of a list; dropped the now-unused `contactSeparatorText` id and `contactSeparator` style (confirmed via `execute_sql` no live CV's Block-tab `nodes` override referenced either). (2) a regular entry's bullet content — replaced the per-`lineGroups`-group `bulletList` repeat (one `<ul>` per group) with a single `repeat` directly over the new `entry.bulletItems`, one shared `<ul>` total; removed the now-dead `bulletList` block and `entryLineGroupsContainer` style. (3) Skills — new `skillCategoryLine`/`skillCategoryList` block pair renders one bullet per category ("Languages: Go, TypeScript, Java" style, `entryKeyword` reused as the merge repeat's schema-required-but-unused filler block, mirroring `batch1-demo.ts`'s already-proven pattern); `section` block's kind branch became 3-way (`skill` → `skillCategoryList`, `language`/`interest` → existing tight `entryBlock` repeat, else → existing spaced `entryBlock` repeat, both ids unchanged); `entryBlock`'s now-unreachable `kind === "skill"` branch and the `entryKeywordsMuted` style it alone used were deleted. `stylesSchema`/`blocksSchema` updated to match (new entries for the 2 new blocks, removed entries for what got deleted). One real CV (`55a52d69…`, "Test (Copy)") had a Style-tab override on the now-removed `entryLineGroupsContainer` — confirmed via `execute_sql` before starting that no CV had a `nodes` (Block-tab) override on any id being removed; the one stale `styles` override goes inert (harmless dangling jsonb key), every other override on that CV and one other (`4c191617…`) targets a style that still exists and keeps applying. **Verified live in browser** against all 4 real classic CVs (not synthetic data): Skills renders as one bulleted "Category: skill, skill, ..." line per category (one CV showed all 4 real categories — Languages/Frameworks & Libraries/Databases & APIs/DevOps & Tools — from Batch 1's sample data), Experience/Volunteering/Projects entries with multiple line groups (responsibilities + highlights) render as one shared `<ul>`, the header contact line renders as merged "email \| phone \| location \| ..." plain text, both live-override CVs still render their overrides correctly (red name color, header sizing). Zero console errors on all 4 pages. `npm run typecheck`/`lint` clean throughout. **Three live follow-up fixes from user review**: (1) real renderer bug found and fixed in `template-node-renderer.tsx`'s `renderElement` — a leaf text node with a `styles` but no `tag` took an early `if (!tag) return content` shortcut that returned the raw string *before* `cssStyle` was ever applied, silently dropping the style; now falls back to a bare `<span style={cssStyle}>` when a style is present (shared by all templates/backends, not Classic-specific — this is what made the new Skills category label render unbold despite `styles: "entryTitle"`, `fontWeight: 600`). (2) User caught that "work description + responsibilities + highlight will be on same parent `<ul>`" only merged responsibilities+highlights, not the entry's own description — `entry.summary` wasn't part of `lineGroups` at all. Fixed at the data layer: `toEntry` now prepends `summary` (when present) as the first item of `bulletItems`, so it's the list's first bullet; `classic.ts`'s `entryBlock` regular branch dropped the now-redundant separate `entrySummary` `<p>` (and the now-unused `entrySummary` style/schema entry). (3) User flagged the bullet `<ul>`s visually indented relative to the entry title/date text above — `bulletListContainer`'s `paddingLeft: 12` removed so bullets sit flush left, same left edge as the text above (affects both entry bullets and the Skills category lines, both use this style). All three re-verified live (zero console errors), `npm run typecheck`/`lint`/`build` clean after each. **Follow-up feature request, same session**: Work entries gained 4 new rendered fields, all previously captured but not displayed. Location/workplace type/employment type were already written into `inventory_items.details` end-to-end by the Work form (`item-dialog.tsx`) but never rendered by any template or exposed in `FIELD_REGISTRY`; "skills used" was fully wired to `ResumeEntry.skills` (via `item_skills`) and already had a `FIELD_REGISTRY` toggle, but no template rendered it either — in both cases the gap was template-rendering only. `classic.ts`'s `entryBlock` regular branch gained two new nodes: a meta line joining `entry.details.location`/`.workplaceType`/`.employmentType` with `" · "` (renders nothing if all three are absent — generic across kinds since only Work's form writes those `details` keys) placed after the subtitle row, and a "Skills: React, Node.js, ..." line (same `entryKeyword`-filler `repeat.merge` pattern as the Skills category lines) placed after the bullet list. New styles `entryMetaLine`/`entrySkillsLine`/`entrySkillsLabel` (+ `stylesSchema` entries). `FIELD_REGISTRY.work` gained `location`/`workplaceType`/`employmentType` toggle entries (Visibility tab) — confirmed live all 3 plus "Skills used" appear as toggle rows under Experience. Verified live against real data (9 Work entries, all already carrying real `location`/`workplaceType`/`employmentType` values and linked skills) — every entry renders its meta line and skills line correctly, zero console errors, `npm run typecheck`/`lint`/`build` clean. **ATS-review pass, same session**: user pasted a third-party ATS reviewer's critique of an exported PDF and asked to fix it — 5 concrete issues, applied to `classic.ts`'s `entryBlock` `else` branch only (education/project/award/etc. keep their existing layout except the URL/separator fixes below, which are universal). (1) **Header order** — for `work`/`volunteer` kind specifically (the only kinds with a "Position"-shaped `subtitle`, confirmed via `FIELD_REGISTRY`), the heading now reads `"{Position} | {Company} — {metaLine}"` with the date isolated onto its own line below, instead of Company+date sharing a row and Position relegated to a second line. (2) **`repeat.merge`/`join` separators changed from `" · "` to `", "`** everywhere in `classic.ts` (education's subtitle+studyType row, the language/interest branch's subtitle prefix) — mid-dots risk mangling in some PDF-to-text extractors. (3) **`entryMetaLine` redesigned**: new `ResumeEntry.metaLine: string | null` field (`persona.ts`'s new `buildMetaLine` helper) renders `"{location} ({workplaceType}, {employmentType})"`, dropping `workplaceType`/`employmentType` when they equal the implied-default `"on-site"`/`"full-time"` (case-insensitive) — e.g. `"Jakarta, ID (Hybrid)"`, or just `"Jakarta, ID"` when both are default. Rendered inline in the Work/Volunteer heading, not its own line. (4) **Bare URL line removed everywhere** (not just Work/Volunteer) — `entryUrlLine` style/node deleted; the entry's primary heading text (company name for Work/Volunteer, `title` for every other kind) is wrapped in `<a href>` when `entry.url` is present instead. (5) **Trailing "Skills: ..." line removed** — user confirmed via `AskUserQuestion` (three options: remove/keep/fold-into-meta-line) after flagging that auto-bolding skill mentions inside free-text bullets, the reviewer's suggested alternative, isn't reliably buildable in this pass (fragile substring matching against arbitrary bullet text); `FIELD_REGISTRY.work`'s `skills` toggle deliberately left in place (pre-existing before this session, matches its prior not-yet-rendered state). Net removed from `classic.ts`: `entryUrlLine`/`entrySkillsLine`/`entrySkillsLabel` styles+schema; `entryMetaLine` style kept but reworked (`fontWeight: 400` added since it's now nested inside the bold `entryTitle`-styled heading). Verified live against both real multi-entry CVs (`1f309c70…`, `0680e6bc…`): every Work/Volunteer entry (including one with no `url`/no notable `workplaceType`/`employmentType`, e.g. plain `"Jakarta, ID"`) renders correctly, company/org names and Education's institution are real `<a href>` links (confirmed via accessibility snapshot, not just visual), no bare URL or "Skills:" lines anywhere, the live style-override CV (`55a52d69…`) still renders its overrides correctly against the new structure. Zero console errors across all checks. `npm run typecheck`/`lint`/`build` clean. |
| 22 | Remove the PDF rendering backend | [specs/07](specs/07-cv-template-pdf-format.md) | — | Done | Confirmed dead since Batch 1 (row 19) — `cv-print.tsx` has printed via native `window.print()` (`react-to-print`) over the DOM renderer since before Batch 3 — this row deletes it outright rather than leaving it as unreachable code. Removed: `src/components/cv/template-pdf-renderer.tsx` (the whole file); the `@react-pdf/renderer` dependency (`npm uninstall`, `package.json`/lockfile); and, confirmed via grep to have zero effect in the DOM renderer and zero use by any template, the pagination-only schema surface that existed solely for that backend — `ElementNode.fixed`/`.break`/`.wrap` and the `PageNumberNode` node shape (type, zod schema, `template-node-renderer.tsx`'s dispatch branch + `renderPageNumber`, `cv-template-core.ts`'s `collectBlockNodeIds` branch). Doc comments in `cv-template-core.ts`, `template-node-renderer.tsx`, and `cv-template-defs/two-column.ts` that referenced the now-gone PDF backend/react-pdf reworded. Spec 07 amended in place (not forked) with a note at the top plus rewritten "Units"/"Style vocabulary"/"Two backends" (now "One core, one renderer")/"What changes at call sites"/"Out of scope"/"Open points" sections reflecting the single-DOM-renderer reality; spec 08's now-moot "PDF backend doesn't read templateSettings" bullet struck through; plan 10 (the original PDF-backend implementation plan) got an archival note pointing forward to this row, left otherwise intact as the historical record, same treatment as spec 05 relative to spec 07. `npm run typecheck`/`lint` both clean after every edit. |
| 23 | CV export/import (self-contained snapshot) + Applications tracking | [specs/09](specs/09-cv-export-import.md) · [10](specs/10-applications-tracking.md) | [plans/11](plans/11-cv-export-import-and-applications.md) | Done — all 8 phases shipped, typecheck/lint/build clean throughout; not verified live in a browser (skipped per explicit user instruction this session) | Settles spec 01/03's deliberately-deferred "snapshot" open question. **Spec 09 (CV export/import)**: new `src/lib/cv-snapshot.ts` — `CvSnapshotV1` (resolved `ResumeDocument` + fully inlined `TemplateDefinition` + `templateSettings`, no DB/registry lookups needed to render it), `buildCvSnapshot`/`parseCvSnapshot`/`templateFromSnapshot`. `cvs.persona_id`/`template_id` made nullable, new `cvs.snapshot jsonb` + a `cvs_live_xor_frozen` check constraint (never neither, never both). Import (user confirmed via `AskUserQuestion`) produces a **frozen, read-only-content, presentation-editable** CV — not a full Persona/Inventory restore: `cv-list-panel.tsx` gained Import (file picker → `parseCvSnapshot` → `importCvSnapshot`, inline error on failure) and per-row/print-page Export (`downloadCvSnapshot`, split into its own file since `cv-snapshot.ts` is reachable from `scripts/seed.ts`'s Node-only `tsconfig.node.json` program and can't touch `Blob`/`URL`/`document`). `CvFormDialog` gained a `frozen` mode (hides Persona/Template selects); `PersonaFieldTree` hides Visibility/Data tabs for a frozen CV, keeps Style/Page/Block. **A real bug was caught and fixed before any of this could work**: `ResumeRender`/`TemplateRender` always resolved `templateId` through the `cv-templates.ts` registry, silently falling back to `cvTemplates[0]` on an unrecognized id — fatal for a frozen/imported CV's inlined template. Fixed by accepting an optional inline `definition` prop that bypasses the registry lookup entirely when set. **Spec 10 (Applications)**: new `applications`/`application_status_history` tables + `application_status` enum (`draft/applied/interview_call/approved/rejected/archived/withdraw`, flat/any-to-any transitions) — `applications` later also gained a `tags text[]` column (live user follow-up, see below). New `src/lib/application-store.tsx`/`application.ts` (mirrors `persona-store.tsx`/`cv.ts` exactly): `setApplicationStatus` is the freeze mutator — **user explicitly clarified mid-session that the frozen CV must live only in `applications.cv_snapshot`, never as a new `cvs` row and never via the separate `importCvSnapshot`** (a completely different, unrelated feature); the mutator writes `cv_snapshot` (via `resolveCv`+`buildCvSnapshot`) only on the exact `draft`→non-`draft` transition, omitting the key entirely from the update payload on every later status change so nothing ever overwrites the original freeze, and throws if leaving `draft` with no `cv_id` attached. New `src/components/applications/` (`application-form-dialog.tsx`, `application-detail-sheet.tsx` — status `Select` gated + a once-only freeze-confirmation `AlertDialog` mirroring the mutator's exact condition + newest-first history timeline, `delete-application-dialog.tsx`, `application-list-panel.tsx`) and `application-status.ts` (`STATUS_LABEL`/`APPLICATION_STATUSES`); `applications.tsx` replaced its placeholder. New `resolveApplicationCv` (prefers `cvSnapshot` over live `cvId`, discriminated `{kind: "frozen"\|"live"}` so export knows whether to re-serialize the existing snapshot or build a fresh one) backs a new `/applications/:id/cv` route (`application-cv-print.tsx`) — deliberately no Persona settings sidebar, per spec 10's "ResumeRender + the same download action" only. **Live follow-up mid-execution**: user flagged `ApplicationFormDialog` didn't match `CvFormDialog`'s established styling (missing the muted Note+Tags panel, and `note` had a data-layer field with no UI at all) — confirmed via `AskUserQuestion` to add a real `applications.tags` column (new migration, mirrors `cvs.tags`, no registry enforcement) rather than fake it, then wired both `note`/`tags` through the form, `DbApplication`, `ApplicationFormFields`, and both mutators. All migrations applied live against the "Tailored CV" Supabase project via Supabase MCP; `get_advisors` showed zero new findings at every step (only the pre-existing leaked-password-protection warning and the `auth_rls_initplan` pattern already present on every other table). One structural bug found and fixed independent of the two specs: `tsconfig.node.json` was missing the `@/*` path alias present in `tsconfig.json`/`tsconfig.app.json` — latent until `cv-snapshot.ts` became the first file reachable from `scripts/seed.ts` to use it; fixing it then surfaced that `cv-snapshot.ts`'s `ResumeDocument` type lived in `persona.ts` alongside Supabase/store code unsafe for that Node-only program, so the `Resume*` type family was extracted into a new leaf file, `src/lib/resume-document.ts`, with `persona.ts` re-exporting for existing call sites. `npm run typecheck`/`lint`/`build` all clean after every phase and at the end. **Not verified live in a browser this session** — user explicitly asked to skip visual/functional browser checks partway through (Phase 7 onward); Phases 1–6 *were* verified live (export/import round-trip, template-render correctness, store mounting) before that instruction. |
| 24 | Batch 5 — batch "Add to Persona", certificate link field, inline skill creation | [02](specs/02-inventory-data-model.md) · [06](specs/06-persona-cv-split.md) | — | Done — typecheck/lint clean, not browser-verified | Continues the `docs/user-request.md` batch backlog (row 19 = Batch 1, 20 = Batch 3, 21 = Batch 4). Plan captured in-session (no `docs/plans/` artifact), same convention as prior batches. Landed while another concurrent session (`tailor-cv-17`) was independently building row 23's CV export/import + Applications work on the same branch — this batch's commit (`46876d4`) deliberately scoped `git add` to only the 6 files it touched rather than staging broadly, so that session's in-progress uncommitted work was never at risk; verification and this doc update were paused mid-session until that session went idle. Three pieces. **(1) Batch "Add to Persona"**: `pool-panel.tsx`'s bulk-action bar (all 3 buttons were disabled placeholders since row 12) had its "Add to CV" button renamed "Add to Persona" and wired up — Tags/Delete stay disabled, out of this batch's scope. Opens `add-to-persona-dialog.tsx` (built around row 16/17, but orphaned — zero import sites anywhere before this), rewritten from a single-item, disabled-confirm scaffold into a real multi-item batch flow: `items: DbInventoryItem[]` (all one `kind`, in selection order — `pool-panel.tsx`'s `selected` is a `Set`, which iterates in insertion order) plus multi-select target Personas. Confirm diffs each target Persona's existing items (`itemIdsForKind`, moved from a `persona-detail.tsx`-local helper to an exported `lib/persona.ts` function so both call sites share it) against the batch and only adds what's missing — never re-adds, never removes, matching the request's "persona has A,B,C,D; batch is B,C,E,F; only add E,F" example. For `PICK_ONE_KINDS` (Basics single-value kinds — headline, name, summary, contact, location), only the last-selected item in the batch is a candidate, per "select multiple but only the latest sticks"; a Persona that already has every candidate renders disabled + "Already added". Uses the pre-existing `setPersonaSectionItems` mutator unmodified — it already took an explicit final id list, so the union/reduction logic lives entirely in the dialog. **(2) Certificate link field**: `item-dialog.tsx`'s `KIND_FIELDS.certificate` gained a `url` field ("Credential URL"), `persona.ts`'s `FIELD_REGISTRY.certificate` gained the matching Visibility-tab toggle — no template change needed, since Batch 4's ATS pass already made Classic wrap any kind's title in `<a href>` when `entry.url` is present, so certificates get real links for free. **(3) Inline skill creation from "skills used"**: `SkillLinkInput` (Work/Volunteer/Project's skill picker) previously had no create-new affordance — Enter on an unmatched query did nothing, its doc comment said so explicitly. Now Enter with no suggestions opens the Skill `ItemDialog` (`kind="skill"`, `mode="add"`) prefilled with the typed text; `ItemDialog` gained an `initialTitle` prop (seeds `EMPTY_STATE.title`, "add" mode only) and its `onSaved` callback widened from `() => void` to `(item: DbInventoryItem) => void` (backward-compatible — the one real caller, `pool-panel.tsx`, ignores the arg) so `SkillLinkInput` can append the newly-created skill's id to `value` without a second round trip. `npm run typecheck`/`lint` clean. **Not verified in a browser this session** — user asked to skip the visual check. |
| 25 | Application stage timeline (replaces flat status/history) | [10 (amended)](specs/10-applications-tracking.md) | [plans/12](plans/12-application-stage-timeline.md) | Done — all 8 phases shipped, typecheck/lint/build clean throughout; not browser-verified (skipped per explicit user instruction) | Per [docs/user-request/timeline.md](user-request/timeline.md)'s "prioritize this request above the existing spec" instruction, **replaces** spec 10's flat `applications.status`/`application_status_history` pair rather than sitting alongside it. **DB** (migration `20260811020000_add_application_stage_timeline.sql`, applied live against `artmbeeadepxggbvfkdr`): new `global_application_status` enum (6 values) backs a new `applications.global_status` column, backfilled from the old 7-value `application_status` enum (the one real row landed on `draft` → `draft`), then the old column/type/`application_status_history` table are dropped outright. New `stage_templates` (per-user autocomplete registry, `unique(user_id, name)`) and `application_stages` (`parent_stage_id` self-FK with `on delete cascade`, one level of nesting enforced app-side not DB-side, `stage_progress_status` enum, `position`, `scheduled_at`/`completed_at`/`notes`/`interviewer_names[]`) tables, both RLS'd. `applications.current_stage_id` FK added last. `database.types.ts` regenerated; `get_advisors` showed zero new findings (only the pre-existing leaked-password-protection warning). **Data layer**: `src/lib/application-stage.ts` (new) — `stagesForApplication` (the depth-1-nested `StageNode[]` selector), `createStage`/`updateStage`/`deleteStage`/`setCurrentStage`; depth-1 is enforced in `createStage` itself (throws on a `parentStageId` pointing at a stage that already has a parent), not just the UI. `src/lib/stage-templates.ts` (new) mirrors `skill-categories.ts`'s CRUD shape but deliberately has no usage-count/orphan-warning concept — `application_stages.name`/`category` are plain denormalized `text`, never FK'd to the template that suggested them, so renaming/deleting a template can never rewrite a stage already recorded on an application. `application-store.tsx`'s `applicationStatusHistory` array/setter replaced with `applicationStages`+`stageTemplates` (the latter fetched user-wide, not per-application). `application.ts`'s `setApplicationStatus` renamed `setGlobalApplicationStatus` (freeze condition now keys off `globalStatus`, the history-insert block deleted entirely — no table left to write to); `createApplication` no longer bootstraps a history row, since the stage timeline correctly starts empty until a real stage is added. Mechanical rename (`status`→`globalStatus`, `ApplicationStatus`→`GlobalApplicationStatus`, `APPLICATION_STATUSES`/`STATUS_LABEL`→`GLOBAL_APPLICATION_STATUSES`/`GLOBAL_STATUS_LABEL`) swept through `application-detail-view.tsx`/`application-list-panel.tsx`/`application-detail.tsx`/`delete-application-dialog.tsx`; confirmed via grep that zero old-name references survive anywhere in `src/`. **UI**: Settings gained a **Stage Templates** tab (`stage-templates-panel.tsx`+`stage-template-dialogs.tsx`, cloned from Skill Categories' shape, minus the usage-count column/orphan copy — its rename dialog uniquely edits name *and* category together, since a stage template is always the pair). New Add/Edit Stage form (`stage-form-dialog.tsx`) composes `stage-name-input.tsx` (single-value, non-chips `Combobox` suggesting from the template registry, prefix-matched and capped at 5, with an inline "create a new template" dialog on Enter-with-no-match — mirrors `SkillLinkInput`'s pattern), a Category/Status `Select` pair, `deadline-date-picker.tsx` ×2 (gained an optional `label` prop so it's no longer deadline-specific — reused for Scheduled/Completed), `NoteInput`, and a new `interviewer-names-input.tsx` (plain free-text chip list, no registry — unlike `TagInput`/`SkillLinkInput`, there's nothing to suggest). The Timeline tab's "Timeline coming soon." placeholder (`application-detail-view.tsx`) is replaced by `timeline-tab.tsx` + `stage-card.tsx`: a hand-composed connected vertical list (`border-l-2 border-border` rail + dot markers — no existing shadcn timeline/stepper primitive in this project's registry, confirmed via `npx shadcn search`) of stage cards, each with a category/status/"Current" badge row, conditionally-rendered meta rows (dates/notes/interviewer badges), and a nested indented list of depth-1 sub-stage rows whose action menu deliberately omits "Add sub-stage"/"Mark as current" — the UI enforcing the same one-level cap the mutator already enforces. **Deliberately trimmed, per the plan's locked-in decisions**: stages stay manually set and never auto-derive `global_status` (considered and rejected as too ambiguous to encode generically); stage categories are a fixed 11-value code-level list (`custom` as the escape hatch) with no free-text "type your own" control in the shipped UI, even though the DB column stays plain `text` for forward compatibility; no drag-and-drop stage reordering. `npm run typecheck`/`lint`/`build` all clean after every phase and at the end. **Not verified in a browser this session** — user explicitly asked to skip the visual check. |
| 26 | Applications: search/filter, sub-navigation (List/Kanban/Archive), archive replaces delete | [11](specs/11-applications-search-nav-archive.md) | [plans/13](plans/13-applications-search-nav-archive.md) | Done — all 7 phases shipped, typecheck/lint clean throughout, verified live in browser | Per [docs/user-request/application.md](user-request/application.md). **DB** (migration `20260811030000_add_application_archived_at.sql`, applied live against `artmbeeadepxggbvfkdr`, confirmed via `execute_sql`): new nullable `applications.archived_at timestamptz` + `(user_id, archived_at)` index — a soft-delete timestamp, deliberately orthogonal to `global_application_status` rather than a 7th enum value. `database.types.ts` regenerated. **Data layer**: `DbApplication.archivedAt` (`mocks/types.ts`), `mapApplicationRow` in `application-store.tsx` carries it through; `archiveApplication`/`restoreApplication` mutators added to `application.ts` (same shape as the existing `updateApplication` — direct Supabase update + store patch), sitting alongside the now-UI-unwired `deleteApplication` (kept, per spec, as a plausible future hard-delete, not currently called from anywhere). `quill-html.ts` gained `stripHtml` (space-joining `<[^>]*>` strip, so words on either side of a stripped tag don't glue together) for search-matching `vacancyDetail`'s rich-text content. **Search/filter**: `application-list-panel.tsx` reworked to accept an `archived?: boolean` prop, reused for both the List and Archive tabs. Brought the panel's toolbar in line with Inventory's `pool-panel.tsx` pattern (previously the outlier with no search/tag filter at all): `SearchInput`(title + stripped `vacancyDetail`) + `TagFilter` + the pre-existing status `Select`, all `useSessionState`-persisted per-tab (`applications:list:*`/`applications:archive:*` keys). Filter order: archived/non-archived → status → search → tag. Empty-state message names which filter(s) produced zero rows, mirroring `pool-panel.tsx`'s `emptyMessage`. "New Application" hidden on the Archive tab. **Archive replaces Delete**: row actions dropdown — List view shows `Edit`/`Archive` (new `archive-application-dialog.tsx`, non-destructive-styled confirm, "It will be hidden from the applications list until restored from Archive."); Archive view shows `Edit`/`Restore` (no confirmation, since restoring is non-destructive). `delete-application-dialog.tsx` left as a file but no longer imported here. **Kanban** (new `application-kanban-panel.tsx`): view-only, no drag-drop (no kanban/dnd library added or needed) — one always-rendered column per `GLOBAL_APPLICATION_STATUSES` entry (even when empty, so the pipeline shape is visible), `Card`-based rows (Title/Company/Position/updated date) that open the same `ApplicationDetailSheet`/`ApplicationFormDialog` wiring the list panel uses, self-contained (own `selectedId`/`editTarget` state) rather than lifted to the page. Excludes archived applications. **Sub-nav**: `applications.tsx` rewritten to the same `Tabs`/`TabsList variant="line"` shape `cv.tsx`/`settings.tsx` already use — three tabs, "Applications List"/"Kanban"/"Archive", plain `defaultValue` state (not URL-routed, matching precedent) — no `App.tsx` route changes. **Verified live in browser** (dev server + Chrome DevTools MCP, zero console errors throughout): all three tabs render correctly against real data; search narrows by title keyword with the correct empty-state copy; full Archive→confirm→disappears-from-List/Kanban→appears-in-Archive→Restore→reappears-in-List round trip confirmed against the live Supabase project; Kanban's 6 columns and counts render correctly. `npm run typecheck`/`npm run lint` clean after every phase. |
| 27 | URL-synced dialog/drawer/tab state (app-wide) | [12](specs/12-url-synced-ui-state.md) | [plans/14](plans/14-url-synced-ui-state.md) | Done — all 9 phases shipped, typecheck/lint clean, spot-verified live in browser | Every top-level `Tabs`, the app's one drawer (`ApplicationDetailSheet`), and every dialog/`AlertDialog` across Applications, Personas, CV, Settings, and Inventory now store their open/active state in the URL (`useSearchParams`, `react-router@8.3.0`, no new dependency) instead of local `useState`/uncontrolled `Tabs defaultValue` — deep-linkable, refresh-safe, Back/Forward-aware. **Two new shared hooks** (`src/hooks/`, same file-per-hook convention as the pre-existing `use-session-state.ts`): `use-tab-search-param.ts`'s `useTabSearchParam(param, defaultValue)` feeds straight into `Tabs value`/`onValueChange` (fills in the default via a `{replace: true}` effect so the first paint doesn't push a spurious history entry, every real switch pushes normally so Back steps through tab history); `use-dialog-search-params.ts`'s `useDialogSearchParams()` gives every dialog the same `dialog`/`get`/`open`/`close` contract (`open` sets `dialog` + companion params and pushes history, `close` removes exactly the keys it's told to and leaves unrelated params like a page's `tab` untouched). **Param scheme**: page tabs use `?tab=`; a component's own nested Tabs that could collide with a page-level `tab` (e.g. `ApplicationDetailView`'s Job Detail/CV Preview/Timeline, rendered both in the Applications-list drawer and standalone on `/applications/:id`) gets its own name (`detailTab`) instead; every dialog uses `?dialog=<name>` plus whatever id-shaped companion params it needs (`id`, `stageId`, `kind`, `status`, `ids` for bulk-select). Shipped in 9 phases (1 sequential foundation + Applications reference wiring, 2–3 the rest of Applications including the once-only nested stage/sub-stage dialogs, 4–8 Personas/CV/Settings/Inventory/CV-print in parallel since they touch disjoint files, 9 final sweep) — full file list and exact param names are in plan 14. **Deliberately stayed local state, by design, not oversight**: form field contents everywhere (only open/closed + which record moves to the URL, per spec); a handful of dialogs nested two levels deep inside an already-URL-addressed dialog (`NewStageTemplateDialog` inside `StageFormDialog` inside a stage's own edit/add dialog; the inline skill-creation dialog inside `SkillLinkInput`; `ItemDialog`'s own unsaved-changes confirm) — same precedent the spec set for `item-dialog.tsx`'s internal line-kind tabs; Inventory's bulk "Add to Persona" selection (an array of item ids, not URL-shaped) and `ItemDetailDialog`'s one-time scroll-to-usage flag (scroll position, explicitly out of scope). **One edge case flagged, not fixed**: `useTabSearchParam`'s default-fill effect only fires while its param is *absent* from the URL — if `persona-field-tree.tsx`'s frozen/unfrozen default ever needed to change for an already-mounted instance with `tab` already present (e.g. a CV live-flips from unfrozen to frozen without remounting), the stale tab value wouldn't self-correct; judged unlikely in practice (freezing happens via a status change that the app already treats as page-navigation-worthy) and left as-is rather than complicating a hook shared by 6+ call sites. `npm run typecheck`/`npm run lint` clean after every phase and at the end (each phase ran an isolated agent, verified in a shared/dirty working tree by scoping checks to its own files, since 5 of the 9 phases ran concurrently in the background across disjoint file sets); one true full-repo pass at the end confirmed zero cross-phase interference. **Spot-verified live in browser** (dev server + Chrome DevTools MCP): `/applications` loads with `?tab=list` filled in via replace (no junk history entry), switching to Kanban updates the URL live, clicking a row opens the drawer with `applicationId`+`detailTab` appended while `tab` is preserved, zero console errors throughout. Phase 7's agent additionally browser-verified the full Inventory dialog set (view→edit handoff, add/delete/add-to-persona) live against `/inventory/basics` and `/inventory/skills`. Not every one of the ~20 converted components was individually re-verified in a browser this session — typecheck/lint plus the scoped per-phase greps stand in for the rest. |
| 28 | Save as new template | [specs/13](specs/13-save-as-new-template.md) | [plans/15](plans/15-save-as-new-template.md) | Done — Phases 1–5 shipped, typecheck/lint/build clean; Phase 6's `bakeTemplateSettings` fixture test passed against the real Classic definition; no browser check (not requested, not visual/theming-specific) | New `cv_templates` table (migration `20260811123121`, RLS `own cv_templates`, `user_id` index) stores a user's saved layouts as complete, standalone `TemplateDefinition`s — never a delta on a built-in, matching every built-in's own self-containment rule. New `bakeTemplateSettings` (`src/lib/cv-template-bake.ts`) flattens a CV's Style/Page/Block `template_settings` onto its base template into one such standalone definition, mirroring `template-node-renderer.tsx`'s override semantics exactly — including the one subtle case that isn't a plain in-place tree patch: a `BlockInstanceNode`'s own `styles`/`text` override only affects *that call site*, not other instantiations of the same block by name, so baking clones the block under a private name and repoints just that call site rather than mutating the shared block definition (verified directly: `entryBlockRepeatTight`/`entryBlockRepeatSpaced`, two different repeats referencing Classic's shared `entryBlock` block, bake independently; `sectionHeadingInstance`'s style override clones `sectionHeading` without touching the original). CV edit page (`persona-field-tree.tsx`) gained a "Save as new template" button next to the Style/Page/Block tabs, shown once `cv.templateSettings` has any override, opening `SaveAsNewTemplateDialog` (name + description) → `saveAsNewTemplate` (`cv.ts`) → new `cv_templates` row; the CV being edited is left untouched (still on its original base template, still further editable) — this is a copy-out, not a rebind. `PersonaStore` now loads `cv_templates` alongside `cvs`/`personas`; `findTemplate`/`allTemplates` (`cv-templates.ts`) check the built-in registry first, then a user's saved rows, and replaced direct `cvTemplates.find` at every render/print/picker call site that resolves a template purely from an id (`resolveCv`, `TemplateRender`, `CvListPanel`'s row resolution + template picker). **Deliberately left untouched**, per the plan: the Templates gallery (`templates-panel.tsx`) stays built-ins-only — it's an ad hoc unsaved-preview surface, saving is already a deliberate action from the CV edit page, not something to browse for; the two Persona-page preview pickers (`personas.tsx`, `persona-detail.tsx`) use `cvTemplates` for an unrelated ad hoc preview, out of scope; no rename/delete/re-edit of a saved template (save-only in this pass). Committed in 5 phase-scoped commits on `native-print`. |
| 29 | Missing skills check (Application/CV/Persona) | [specs/14](specs/14-missing-skills-check.md) | [plans/16](plans/16-missing-skills-check.md) | Done — all 7 phases shipped, typecheck/lint clean throughout; no browser check (not requested, not visual/theming-specific) | New `required_skills_input`/`missing_skills` columns on `applications` (migration `20260815215833_add_application_skill_check.sql`, applied live against `artmbeeadepxggbvfkdr`, `database.types.ts` regenerated, `get_advisors` showed no new findings). **Shared**: `src/lib/skill-check.ts` — `findMissingSkills` (exact, case-insensitive, trimmed line-diff, dedupes input) and `skillTitlesOf` (flattens a resolved `ResumeDocument`'s `skill` section across `skillGroups`). `src/components/skills/skills-check-dialog.tsx` — a controlled `SkillsCheckDialog` (textarea + Check button + inline destructive-badge result) that never fetches or persists; callers own `value`/`result` state and an optional `extraFooter`/`disabledReason`. **Three call sites**: Application detail (`application-detail-view.tsx`) — persisted; a "Check skills" trigger + `MissingSkillsField` (destructive `Badge`s with a `TriangleAlert` icon) next to CV/Apply via, comparing against `resolveApplicationCv(...).document`'s skills, with the dialog's own Save button writing both columns via `updateApplication` (`ApplicationFormFields`/`mapApplicationRow` extended accordingly; `application-form-dialog.tsx` deliberately untouched — no longer where this input lives). CV page (`cv-print.tsx`) — ephemeral, plain `useState`, compares against the already-resolved `document`'s skills (so it reflects that CV's own field-visibility overrides), button in the header next to Export/Print. Persona detail (`persona-detail.tsx`) — ephemeral, plain `useState`, compares against the persona's full `skillTitlesOf(document)`; `SectionCard` gained an optional `extraAction` slot (rendered in `CardAction` next to `PickButton`) used only by the Skills card. `npm run typecheck`/`npm run lint` clean after every phase. |
| 30 | CV embedded in Applications (removes standalone CV page/`cvs` table) | [specs/15](specs/15-cv-embedded-in-applications.md) | [plans/17](plans/17-cv-embedded-in-applications.md) | Not started | Collapses the standalone `cvs` table + `/cvs` page into `applications` itself: 4 new columns (`cv_persona_id`, `cv_template_id`, `cv_persona_settings`, `cv_template_settings`) replace `cv_id`; the application detail view's CV Preview tab becomes an editable "CV" tab (Persona+Template picker on first open, then inline field-visibility/style editing, frozen read-only once the application leaves `draft`). Templates system (`cv_templates`) kept, gallery page dropped. Export/Duplicate-CV/Favorite dropped; Import reworked to copy CV settings from another live application instead of a file. Plan also found and scoped in two spec gaps: `application-form-dialog.tsx`'s existing CV picker field (removed) and `personas.tsx`'s own New-CV flow (removed). 8 phases, not yet executed. |
| 31 | Inline persona create/edit from the Applications CV tab | [specs/16](specs/16-inline-persona-editing-in-cv-tab.md) | [plans/18](plans/18-inline-persona-editing-in-cv-tab.md) | Not started | Adds a "+ Create new persona…" option to `ApplicationCvSetup`'s Persona picker (opens the full Persona editor as a popup, auto-selects on create) and an "Edit persona" action in `PersonaFieldTree`'s Data tab (opens the same popup on the application's current persona). Extracts the standalone `/personas/:id` page's content into a reusable `PersonaEditorPanel` (takes `personaId` + a `dialogParamPrefix` instead of route params) shared by the route page and a new `PersonaEditorDialog`. 5 phases, not yet executed. |
